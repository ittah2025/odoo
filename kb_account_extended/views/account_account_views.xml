<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_account_form_inherit_kb" model="ir.ui.view">
        <field name="name">kb.account.account.form.inherit</field>
        <field name="model">account.account</field>
        <field name="inherit_id" ref="account.view_account_form"/>
        <field name="arch" type="xml">
            <field name="allowed_journal_ids" position="after">
                <field name="is_anl_required"/>
                <field name="is_partner_required"/>
                <!-- <field name="is_anl_tag_required"/> -->
            </field>
        </field>
    </record>
</odoo>
